<div class="system-decision" role="img" aria-label="The existing Google Sheet remains the system of record. The merchandising application reads from it to provide timeline, brand, comparison, search, and history views, with controlled edits written back to the same source.">
  <div class="source-card">
    <span>Existing official record</span>
    <strong>Google Sheet</strong>
    <small>Upstream data continues to flow here</small>
  </div>
  <div class="connection" aria-hidden="true"><span>Read</span><i></i><span>Controlled updates</span></div>
  <div class="experience-card">
    <span>Redesigned experience</span>
    <strong>Marketing Planner</strong>
    <ul><li>Timeline</li><li>Brand views</li><li>Search</li><li>Year over year</li><li>Creative history</li></ul>
  </div>
</div>

<style>
  .system-decision { display: grid; grid-template-columns: minmax(0, 0.38fr) 7rem minmax(0, 0.62fr); gap: var(--space-4); align-items: center; padding: var(--space-6); border: 1px solid var(--color-border-strong); border-radius: var(--radius-md); background: var(--color-surface); }
  .source-card, .experience-card { padding: var(--space-5); border: 1px solid var(--color-border); border-radius: var(--radius-sm); }
  .source-card { background: var(--color-canvas); }
  .experience-card { border-color: color-mix(in srgb, var(--color-accent) 45%, var(--color-border)); background: color-mix(in srgb, var(--color-accent) 5%, var(--color-surface)); }
  .source-card > span, .experience-card > span { display: block; margin-bottom: var(--space-2); color: var(--color-text-muted); font-family: var(--font-mono); font-size: 0.62rem; letter-spacing: 0.07em; text-transform: uppercase; }
  .source-card strong, .experience-card strong { display: block; font-size: var(--step-1); }
  .source-card small { display: block; margin-top: var(--space-3); color: var(--color-text-muted); line-height: 1.4; }
  .experience-card ul { display: flex; flex-wrap: wrap; gap: var(--space-2); margin: var(--space-4) 0 0; padding: 0; list-style: none; }
  .experience-card li { padding: 0.2rem 0.55rem; border: 1px solid var(--color-border); border-radius: 999px; color: var(--color-text-soft); font-size: var(--step--1); }
  .connection { display: grid; gap: var(--space-2); justify-items: center; color: var(--color-text-muted); font-family: var(--font-mono); font-size: 0.58rem; text-align: center; text-transform: uppercase; }
  .connection i { display: block; position: relative; width: 100%; height: 1px; background: var(--color-accent); }
  .connection i::before, .connection i::after { position: absolute; top: 50%; width: 0.45rem; height: 0.45rem; border: 1px solid var(--color-accent); border-radius: 50%; background: var(--color-surface); content: ""; transform: translateY(-50%); }
  .connection i::before { left: 0; }
  .connection i::after { right: 0; }
  @media (max-width: 42rem) {
    .system-decision { grid-template-columns: 1fr; }
    .connection { grid-template-columns: 1fr auto 1fr; align-items: center; }
    .connection i { width: 1px; height: 3rem; }
  }
</style>
